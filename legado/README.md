# Site legado (HTML estático)

Arquivos do site antigo que ficavam na raiz do repositório e faziam o GitHub Pages publicar o HTML antigo em vez do build React.

O site em produção vem de `frontend/dist` via workflow **Deploy frontend (GitHub Pages)**.
